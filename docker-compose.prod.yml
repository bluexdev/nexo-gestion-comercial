services:
  postgres:
    restart: unless-stopped
  backend:
    restart: unless-stopped
    environment:
      NODE_ENV: production
  frontend:
    restart: unless-stopped
  nginx:
    restart: unless-stopped
